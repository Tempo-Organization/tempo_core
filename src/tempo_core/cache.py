
# ------------------------------------------------------------------------------------------------------------------------------------------------------

# Usage: tempo cache [OPTIONS] <COMMAND>

# Commands:
#   clean  Clear the cache, removing all entries or those linked to specific packages
#   prune  Prune all unreachable objects from the cache
#   dir    Show the cache directory

# Cache options:
#   -n, --no-cache               Avoid reading from or writing to the cache, instead using a temporary directory for the
#                                duration of the operation [env: TEMPO_NO_CACHE=]
#       --cache-dir <CACHE_DIR>  Path to the cache directory [env: TEMPO_CACHE_DIR=Z:\tempo_cache]

# ------------------------------------------------------------------------------------------------------------------------------------------------------

# param > env var > default behaviour
